# --- API Endpoints ---
# Local dev: leave unset to use Vite proxy (see vite.config.js).
# Production / direct backend: set the full API base URL.
# VITE_API_URL=http://localhost:8000

# --- App Settings ---
VITE_APP_TITLE=Logara AI
VITE_APP_ENV=development
